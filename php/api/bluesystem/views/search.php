<?php

$app->get('/search/:text', function($query) use ($app) {
    global $bluesystem;
});


$app->get('/search/business/:id/workorder/:text', function($query) use ($app) {
    global $bluesystem;
});

?>